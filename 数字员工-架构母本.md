# 数字员工 · 架构与理念母本

最后核对：2026-07-17

> 这份文档是**可复用的蓝图**，不是 RedBeacon 的产品说明书。它把 RedBeacon 这套“小红书运营数字员工”里已经跑通的设计理念、代码骨架、客户端内核、平台对接和发布流程抽象出来，给下一个数字员工项目复用。
>
> 读法：每节先看**【原则】**，这是新项目该照抄的部分；再看**【RedBeacon 实例】**，这是本仓当前代码里的落地样子。开新项目时复制原则，替换实例。
>
> 当前参照真源：`AGENTS.md`、`README.md`、`docs/download-node-*`、`release/release-contract.json`、全局 `bytestaff-digital-employee-publish` Skill、`cli/pyproject.toml`、`cli/uv.lock`、`cli/src/redbeacon/core/`、`cli/src/redbeacon/composition.py`、`cli/src/redbeacon/platform_account/`、`cli/src/redbeacon/adapters/ui_backend/`、`cli/src/redbeacon/services/browser_engine.py`、`cli/packaging/RedBeacon.spec`、`cli/packaging/build-versions.env`、`cli/packaging/build_macos_local.sh`、`cli/packaging/build_windows_local.ps1`、`tools/setup_windows_build_vm.ps1`、`tools/build_desktop_local.sh`、`install/`。
>
> 2026-07-14 起，这份母本把“机器可解析交付物”视为客户端的一等架构边界：平台结算前验收、客户端入站规范化、产生副作用前重验，三层缺一不可。
>
> 2026-07-16 起，这份母本同时固定一套已在 Windows / macOS 客户端上验证过的参考实现，以及对应的故障图谱。Linux 兼容策略仍需设计，但只有建立真实 Linux 构建节点并通过同等级 smoke 后，才能宣称支持。参考实现可以替换具体框架，但不能丢掉它背后的所有权、隔离、事务、验收和回滚契约。

---

## 0. 一句话定义：什么是数字员工

**数字员工 = 用户像带下属一样用大白话指挥，软件把一整条专业工作流全包。**

它通常同时长出三张脸：

1. **本机客户端 UI**：用户直接看、点、审、改、发布，是软件的“家”和主入口。
2. **AI skill**：让 Codex、Claude Code 等 AI 助手有一双能看数据、能动手的手。
3. **CLI / 命令入口**：给 skill、脚本和高级用户调用的稳定能力接口。

但背后只允许有**一套业务核心和一份业务数据真源**。UI、skill、CLI 都是同一个员工的不同入口，不能各自长成三套产品。

远期形态是**数字员工市场**：中央身份、权益、算力点、计量网关共用；每个员工只是不同工作流的本机客户端 + skill + 平台能力接入。客户端永远不持上游大模型 key，值钱的身份、扣点、AI 能力统一走平台。

---

## 1. 四条底层理念

**【原则】**

1. **数据是锚。** 软件存在的意义是更好地操作数据。UI / skill / CLI / 未来 API 都只是操作同一份数据的不同投影。判断一段代码健不健康，先看它有没有通过核心读写同一份真源。
2. **核心不是 CLI，而是用例函数。** CLI 只是把参数翻译成调用、把结果渲染成文字。UI 不能拼 CLI 命令，skill 不能靠爬 CLI 文本来做业务判断。同一件事只能在 core 用例里实现一次。
3. **人提供判断，AI 提供执行。** 用户的价值不在“多点几下”，而在方向、判断、品味、取舍。软件要让用户看见成品、随时否决、低成本修正。
4. **内容共创需要共同画布。** 图形界面的价值不是漂亮，而是让用户不用先学会提问，就能看到自己能做什么，并在渲染后的结果上判断“好/不好”。

这四条是代码评审的裁决标准。违反它们不是风格问题，是架构债。

**【RedBeacon 实例】**

RedBeacon 现在的核心事实是：客户端 UI、`redbeacon` / `redbeacon-test` 命令、AI skill 都操作同一套本机业务数据；默认数据源是本机 SQLite，飞书云端源是可选高阶路径，不再是主流程。

---

## 2. 两个入口模型：UI 保下限，skill 提上限

**【原则】**

- **UI 是主角。** 用户没有任何 AI 助手，也应该能独立完成主要流程。UI 负责看板、审核、模板微调、状态感知、可视化确认。
- **skill 是上限。** AI 助手适合联网调研、把自然语言变成结构化动作、批量处理、判断下一步。skill 应围绕 UI 和同一套数据工作，不另立山头。
- **CLI 是稳定接口。** CLI 服务于 skill、脚本、排障和高级用户，不应该成为普通用户必须理解的主入口。

**双向指路：**

- skill -> UI：信息密度高、需要用户可视化判断、命令反复自愈不了、或超出 CLI 覆盖时，直接深链打开对应页面。
- UI -> skill：高阶页面可以放轻量、可关闭的“复制给 AI 助手”提示词，但不能把软件能力建立在用户必须会用某个 AI 助手之上。

**【RedBeacon 实例】**

RedBeacon 的当前主链路是：

**安装 / 更新 -> 平台登录 -> 建账号 -> 小红书扫码登录 -> 定位 / 选题 -> 生成 -> 本机审核 -> 发布**

客户端能独立跑完整链路；AI skill 负责提高选题、定位、批量处理和提示词调优的效率。

---

## 3. 代码骨架：六边形架构，依赖只能向内

**【原则】**

```text
        CLI 适配器 ─┐
     skill 适配器 ─┤
      UI 后端适配器 ─┼──► core（领域 + 用例，纯业务）──► ports（抽象接口）
   未来 API 适配器 ─┘                                      ▲
                         infra（平台 / 本地库 / 云表 / 浏览器 / 文件）
                         composition（唯一选择 infra 并注入 core 的地方）
```

**层级职责：**

| 层 | 装什么 | 铁律 |
|---|---|---|
| `core/domain` | 领域模型、状态枚举、结构不变量 | 纯数据，不认框架 |
| `core/ports` | 核心需要的外部能力，用 `Protocol` 声明 | 只 import domain |
| `core/usecases` | 业务用例，编排、判断、流转都在这里 | 只 import domain + ports |
| `core/presets` | 默认模板、风格、提示词骨架 | 纯数据，不 import 框架 |
| `infra` | 端口实现：平台、本地库、云表、浏览器、文件 | 可以碰 HTTP / DB / 自动化 |
| `composition` | 组装根，唯一决定用哪个 infra | 适配器通过它拿端口 |
| `adapters` / `routers` | UI 后端、CLI 路由、skill 桥 | 只解析入参、调用 core、渲染出参 |

**【RedBeacon 实例】**

- `cli/src/redbeacon/core/usecases/` 里有账号定位、选题、生成、审核、发布、看板等用例。
- `cli/src/redbeacon/core/ports.py` 定义 `Copywriter`、`ReviewWriter`、`TopicAdminPort`、`XhsPublisherPort` 等端口。
- `cli/src/redbeacon/composition.py` 统一注入本机 SQLite 或飞书适配器。默认本机源；切飞书时 core 用例不改。
- `cli/src/redbeacon/adapters/ui_backend/app.py` 的端点基本都走 `uc_*` 用例；这是正确方向。

**当前需要警惕的维护点：**

仓内还有一些历史注释和命名仍写着“Feishu/飞书”，但实际端口已经能同时代表本机源和飞书源。后续改代码时以 `composition.py` 和 AGENTS.md 的当前事实为准，不要被旧注释带回飞书主流程。

### 3.1 已验证的参考技术栈

这是一套适合“Python 业务核心 + 本机数据 + Web 桌面 UI + 浏览器自动化 + AI 平台”的参考实现。它不是框架崇拜：新项目可以把原生前端换成 React/Vue，把 pywebview 换成 Tauri/Electron，但必须保留同样的核心边界、数据所有权、通道隔离、运行时接管和发布事务。

| 领域 | RedBeacon 已验证选型 | 在架构里的职责 |
|---|---|---|
| 语言与运行时 | Python 3.12 构建，项目声明 Python 3.11+ | 领域核心、CLI、本机服务、安装自检、自动化编排 |
| 类型与边界校验 | dataclass + Pydantic 2 | 领域对象、HTTP 请求、机器交付物边界校验 |
| 本机业务数据 | 标准库 SQLite，外键约束、迁移、WAL | 账号、定位、选题、方案、审稿、归档等业务真源 |
| 跨进程调度 | 独立 SQLite 调度库 | checkin 缓存、并发租约、令牌桶、重试、熔断和请求状态 |
| 本机 API | FastAPI + Uvicorn，只监听 `127.0.0.1` | 给桌面前端提供薄 API；通过一次性 UI token 防同机误调 |
| 前端 | 原生 HTML + CSS + JavaScript，无远程资源 | 工作台、状态管理、轮询、进度、预览与用户确认 |
| 桌面外壳 | pywebview + 系统 WebView | 把同一网页装进本机窗口；壳失败时可退回本机浏览器 |
| HTTP 客户端 | httpx / requests 的统一平台封装 | device flow、checkin、AI 请求、超时、错误映射与幂等 |
| 小红书自动化 | CloakBrowser 主路径，Playwright Chromium 兜底 | 扫码登录、发布、持久化 profile 和死会话重建 |
| 卡片渲染 | Playwright Chromium + 独立渲染器进程 | 把 Markdown/HTML/CSS 渲染为封面和正文 PNG，隔离崩溃 |
| 图像处理 | Pillow，含 EXIF 方向和 ICC -> sRGB | 参考图归一化、AI 图片纯像素重写和元数据清除 |
| 配置与资源 | YAML + 包内 HTML/CSS/图片资源 | 集中预设、主题、模板；客户端启动不依赖外部 CDN |
| 桌面打包 | PyInstaller，一次 Analysis 产出 GUI / CLI / Renderer | 自包含 Python 和依赖，不要求客户机安装开发工具 |
| 依赖锁定 | `pyproject.toml` 精确直接依赖 + `uv.lock` 全依赖树 | 测试版与正式版用 `uv sync --frozen` 得到同一依赖集合 |
| 测试 | pytest + 每个支持平台的冻结包 smoke + 安装事务 smoke | 同时验证核心逻辑、入口、辅助进程、浏览器和真实最小产物 |
| 构建与分发 | 同一源码快照在原生系统构建，阿里云 OSS 版本化分发 | 构建节点可本机或 CI；用户下载、更新和 skill 全部走自有 OSS/CDN |

当前经过冻结验证的关键版本记录在项目 `cli/pyproject.toml`、`cli/uv.lock` 和构建工具链版本文件，母本不复制版本号。原因是版本号会升级，真正不可丢的是：**精确锁定、测试/正式共锁文件、升级后在每个支持平台重跑真实冻结包 smoke**。

### 3.2 进程拓扑与调用方向

```text
桌面窗口 / 本机浏览器
        │ HTTP + 一次性 UI token
        ▼
127.0.0.1 FastAPI 适配器 ─────┐
                               ├──► core/usecases ──► ports ──► infra
CLI ─► CLI router ─────────────┘                         │
skill ─► 当前通道 CLI                                    ├─ SQLite / 本地文件
                                                        ├─ ByteStaff 平台
                                                        ├─ 浏览器自动化 worker
                                                        └─ 独立 Renderer 子进程
```

调用纪律：

- GUI 前端不直接读 SQLite，不拼 CLI 命令；它只调本机薄 API。
- CLI 和 UI 后端都直接调同一套 core usecase，不互相套壳。
- skill 只调用当前通道 CLI 或打开 UI 深链，不复制业务实现。
- Renderer、浏览器 worker 是可失败的辅助进程；主进程通过结构化输入输出、超时、取消和产物验收管理它们。
- 读 `--help`、`--version`、更新检查等只读入口应延迟初始化业务数据库，不能因为用户目录不可写就连版本号都看不了。
- 冻结 GUI 可能没有真实 stdout/stderr；日志初始化必须容忍 `sys.stdout` / `sys.stderr` 为 `None` 或没有 `.buffer`，再落到产品日志文件。

### 3.3 文件、目录与所有权

不要只约定“存在哪”，还要约定“谁能删、谁能迁移、谁能复用”。推荐拆成四类：

| 类别 | 内容 | 所有权和生命周期 |
|---|---|---|
| 业务数据目录 | SQLite、用户素材、审核稿、归档 | 当前产品当前通道所有；更新保留，普通卸载保留，显式 purge 才删 |
| 平台身份目录 | 设备令牌、账号级 AI 调度库 | 平台账号所有；同一生产平台账号可按平台规范由多个正式员工共享，测试环境必须另根隔离；不能塞进应用安装目录 |
| 可重建运行时目录 | 浏览器内核、profile、渲染缓存、下载分段 | 当前产品当前通道所有；可自检修复，卸载只删自己的目录 |
| skill 目录 | 宿主真正扫描的 `SKILL.md` / 命令文件 | 当前通道所有；安装更新与客户端版本一起事务化，正式/测试互不覆盖 |

本机敏感文件应使用当前用户可读写、其他用户不可读的权限；数据库迁移、备份和恢复必须覆盖**当前全部业务表**。新增一张表时，必须同步更新级联删除、备份、恢复、迁移和完整性测试，不能只让主流程“暂时能写”。

### 3.4 哪些可以替换，哪些不能妥协

可以替换：前端框架、桌面壳、本机 API 框架、数据库实现、CI 厂商、对象存储厂商、自动化引擎。

不能妥协：

1. 一份 core 和一份业务真源。
2. UI、CLI、skill 不复制业务判断。
3. 用户环境按不可信输入处理，产品接管运行时路径和版本。
4. 正式、测试、开发通道身份和数据完全隔离。
5. 安装、更新、skill、外部运行时是一个可回滚事务。
6. 机器交付物经过平台结算、客户端入站、产生副作用前三层验收。
7. 发布先测试版，人工确认后同代码、同锁文件、同打包流程出正式版。
8. 每个长任务有进度、取消、超时、重试边界和人话错误。
9. 每个平台包都要在对应平台上运行冻结产物的真实最小业务 smoke。

### 3.5 当前参考实现的明确边界

范本必须诚实区分“已经验证”和“希望以后具备”：

- 当前交付矩阵是 Windows x64、macOS arm64；Windows ARM64 通过系统 x64 仿真运行同一 Windows 包。Linux 分发和 macOS Intel 当前暂停，不应对外写成已经覆盖。
- PyInstaller 包内置 Python、Python 依赖和 Playwright driver，但浏览器内核在安装期按精确版本下载到产品通道缓存；“自包含”不等于把所有几百 MB 运行时都塞进 zip。
- pywebview 使用系统 WebView，因此仍有最小系统组件前提；壳不可用时可以退回本机浏览器，但不能声称完全不依赖操作系统图形组件。
- 当前代码中的 macOS/Windows 生产签名与公证能力不能仅靠 bundle id 推导；没有证书和 CI 签名步骤时，应明确标为未完成，而不是在发布说明里暗示已签名。
- 当前生成和发布是用户主动触发的前台工作，不承诺后台常驻。定时发布使用外部平台的原生计划字段，不在本机偷偷常驻一个调度进程。
- 本机数据默认不做多设备同步。需要协作或云同步的新员工，应重新选择数据真源，不能在现有本机库上临时叠一层双向同步。

---

## 4. 单一业务核心：防止一个功能写三套

**【原则】**

同一个功能，具体执行逻辑只能有一份：

- 编排放 `core/usecases`。
- 外部能力放 `core/ports` + `infra`。
- CLI / UI / skill / API 都只做翻译和渲染。

允许适配器内联的只有端自己的事：参数解析、HTTP/命令行出参、后台 job 包装、进度推送、日志、备份、安装更新、窗口打开。

**新功能顺序：**

1. 先定义领域数据和端口。
2. 写 core 用例。
3. 用测试桩把用例跑通。
4. 最后接 CLI、UI、skill。

**【RedBeacon 实例】**

生成内容、审核、选题管理、发布列表等已经大体收口到 core 用例。UI 后端不应该自己写 SQL 或复制 CLI 逻辑；如果看到“同一个判断在 UI 和 CLI 各写一份”，要收回 core。

---

## 5. 数据归属：单一真源，不双写

**【原则】**

每个数字员工都必须先回答：业务数据的真源在哪里？

- **本机真源**适合：单机为主、隐私敏感、装好就能用、离线也要能看、协作需求弱。
- **云端协作真源**适合：多人共审、手机端频繁改、跨设备强需求、外部系统天然是主工作台。
- **敏感凭证**永远放本机隐私内核：平台设备令牌、第三方 cookie、用户自己的云表密钥、代理密钥等。

铁律：

- 禁止“本地一份 + 云端一份”双向同步作为常态设计。
- 允许全局二选一切换数据源，但同一时刻一个业务对象只能有一个真源。
- 云表数据要当不可信输入校验；本机数据也不能绕过核心不变量。
- 任何跨进程、HTTP、队列、预览/提交、导入/恢复边界的数据，都要重新构造领域对象并校验不变量。“刚刚是我们自己输出的”不是信任依据，旧客户端、中断任务和用户编辑都可能让它变质。
- 看板数字必须和真源可见数据同口径。没有落真源的中间态要单独报，不能混进“已完成”。

**【RedBeacon 实例】**

当前默认：

- 业务数据：`~/.redbeacon`；测试版：`~/.redbeacon_test`。
- 平台令牌：`~/.bytestaff`；测试版：`~/.bytestaff_test`。
- 账号级 AI 调度：正式版 `~/.bytestaff/ai_scheduler.sqlite3`；测试版在测试身份目录使用独立库。
- 浏览器、扫码 profile、下载分段和渲染缓存都在各自业务数据目录的产品自有子目录中，不读取第三方默认全局缓存。
- 小红书登录态、账号资料、选题、待审稿、归档默认都在本机。
- 飞书源仍可作为可选能力存在，但不是默认 onboarding，也不是主流程。

---

## 6. 平台对接：身份、点数、AI 能力都在服务端

**【原则】**

1. 客户端永不持上游模型 key。唯一平台凭据是 device flow 签发的设备令牌。
2. AI 能力带设备令牌走平台，由平台鉴权、扣点、限流、审计。
3. 平台账号 ID 由服务端从令牌解出，客户端调用平台能力时不传平台 `account_id`，防 IDOR。
4. 鉴权和扣点必须 fail-closed：未登录、点数不足、平台不可达时，不能假装成功或瞎编结果。
5. 只有被产品契约明确标为 optional 的增强能力可以 fail-soft：例如“纯文字卡 + 可选配图”允许稍后补图；如果方案明确要求“AI 封面 + 文字卡片”，两项都是 required，缺任一项都必须阻止入库。
6. `product` / `capability` 是归因标签，不是客户端本地权限开关。权限和点数以服务端为准。
7. 所有平台调用统一 HTTP 封装：统一 base URL、Authorization、错误码映射、超时、`trust_env=False`。
8. 扣点型调用要带 `request_id` 做幂等，失败补偿归平台，客户端不要自己补扣。
9. 扣点成功的判据是“用户要求的必需交付物已通过可验证契约”，不是“上游返回过一段文字”。机器可解析结果必须使用平台白名单输出契约，由平台在结算前验收；无法解析、必需字段缺失或必需内容为空属于**不可恢复交付失败**，由平台在结算前原路回补。
10. “JSON 合法”不等于“业务可直接使用”。字段齐全但表示层多转义了一次、换行风格不统一、标签带多余前缀等，如果能在不猜测内容、不丢信息的前提下确定性还原，属于**可恢复表示偏差**，不应另发 AI 请求，也不应冒充不可恢复失败申请退款。
11. 客户端规范化必须是**字段级、白名单、单层、幂等**的。禁止对整段中文做通用 `unicode_escape` 解码或无条件全局替换；必须保护 Windows 路径、正则表达式、代码片段和用户真正想展示的反斜杠。同一个规范化器连续运行两次，结果必须和运行一次相同。
12. 机器交付物在“平台响应 -> 领域对象”的入站边界验收一次，经过 UI 预览/HTTP 提交、本地队列或旧版数据后，在渲染、入库、发布等副作用前再验一次。第二道闸门不是重复代码，而是防止旧客户端、中断预览和篡改载荷绕过业务不变量。
13. 客户端不能事后自行声明失败申请退款；验收规则必须预先登记并在服务端执行，防止退款接口被滥用。
14. AI 图片中的文字语言也是交付契约：需要中文时统一使用中国大陆通用规范简体中文，保持业务标题和文案原样，禁止繁体、异体字、错别字和伪文字；明确要求无文字时不得额外加字。规则应集中维护，并在内置模板和最终生图请求边界幂等补齐，使历史自定义方案也自动受保护。

**机器交付物三层闸门：**

| 层 | 责任方 | 验什么 | 失败处理 |
|---|---|---|---|
| 结算闸门 | 平台 | 白名单契约、可解析、必需字段、非空交付 | 不结算或原路回补，返回明确上游错误 |
| 入站规范化 | 客户端核心 | 领域类型、长度、枚举、可恢复表示偏差 | 只做确定性无损修复；不调 AI、不额外扣点 |
| 副作用闸门 | 用例/提交端口 | 经预览、队列、导入后的完整业务不变量 | 阻止渲染/入库/发布，保留诊断信息并给出人话提示 |

**【RedBeacon 实例】**

- `platform_account/` 是可迁移骨架：`client`、`login`、`token_store`、`checkin`、`chat`、`errors`、`doctor`。
- 设备令牌默认写共享目录，且正式版和测试版隔离。
- `platform_account/client.py` 明确不发送平台 `account_id`，只加 Bearer token。
- RedBeacon 文案调用使用平台白名单契约 `redbeacon_copy_v1`；平台验收失败返回已回补的 `upstream_error`，客户端按幂等状态机有界重试。
- `core/usecases/generate.py` 在 `parse_copy` 入站时对多转义一层的换行做字段级、幂等修复；`render_from_draft` 在预览提交后、渲染与入库前再重申一次不变量。这两层都不会再调 AI，且有 Windows 路径/代码反斜杠保护测试。
- `config_keys.py` 里已经移除用户自填 AI key 的路线，平台 base URL 走配置项。

---

## 7. 客户端 UI 内核：一套引擎，多种打开方式

**【原则】**

- UI 只有一份：一个本机后端薄壳 + 一个前端入口。浏览器、桌面窗口、打包客户端只是不同打开方式，不 fork 页面。
- 前端可以零框架、零构建，但必须有自己的轻量工程纪律：页面 reset 登记、数据依赖作废、异步进度、错误兜底。
- UI 面向用户必须是人话，不出现内部字段、skill 名、命令名作为主要说明。
- 本机服务只绑 `127.0.0.1`，真实客户端启动时要有一次性口令或等价保护，避免同机恶意页面误调。
- **桌面客户端必须按产品通道单例。** 同一用户会话中正式版只能有一个正式窗口，测试版只能有一个测试窗口，但正式版、测试版和开发版可以彼此并存。单例锁属于“产品 + 通道”，不能做成所有数字员工共用的全局锁。
- **重复启动不是静默退出。** 第二次点击图标时，不再创建本机 HTTP 服务、数据库连接、工作队列或 WebView；它应通过仅限 `127.0.0.1`、带随机口令的轻量唤醒通道通知首个进程，把已有窗口从最小化状态恢复并置前。唤醒失败时仍要由操作系统锁阻止第二实例，不能为了置前失败就放行双开。
- 长任务必须有进度：生成、发布、更新、下载、替换都不能只给一个“正在处理”的死状态。
- **数字员工要以名字承担工作。** 用户安排任务后，状态文案应采用“员工名 + 真实动作 + 完成去向”，例如“RedBeacon 正在理清思路，写这篇文案”“正在画封面”“正在把成稿放进审稿台”。主按钮优先使用“交给 RedBeacon / 让 RedBeacon 再试一次”这类委托语言，不要把核心体验写成“调用 AI / 开始生成 / 处理中”。
- **拟人化不等于假装有意识。** 不虚构心情、疲劳、思考过程或社交关系，不用卖萌掩盖超时，也不在实际仍排队时声称已经开工。伙伴感来自四件事：记得用户交代的上下文、说清此刻在做什么、完成后把结果放到可预期的位置、失败时说明原因和下一步。
- **状态必须和真实业务阶段一一对应。** 同一句“正在写文案”只能在写作调用已开始时出现；排队就明确说在工作清单里，画图、排版、入审稿台、发布和归档分别显示各自动作。界面可以温暖，但不能牺牲可观测性和故障判断。
- **桌面 Web UI 必须有明确的滚动所有权。** `html/body` 和应用外壳锁定在窗口视口内，不承担业务滚动；主内容区使用可收缩的 Grid/Flex 轨道并成为唯一页面滚动容器，侧栏和弹窗只在自身内容确实溢出时独立滚动。所有滚动边界要阻止手势继续传到根页面，避免 WKWebView/Safari 的整窗橡皮筋回弹让固定导航、顶栏和内容一起晃动。
- 预览 -> 用户确认 -> 提交是两段式事务，不是一个可信内存对象的延续。后端收到预览载荷后必须重新构造领域对象、重跑不变量，再启动渲染、入库、发布或其他副作用。修复和校验不能只写在前端。
- 外部平台原生支持定时动作时，计划时间应属于**每个交付物自己的提交命令**，不要藏在全局设置或整批共享状态里。UI 默认立即执行，用户切换后才展开时间控件；前端用于即时提示，核心用例在入队前和真正执行前重验上下界。计划失效必须停下来让用户重选，绝不能悄悄改成立即执行。

**必备模式 A：页面登记表**

每页把初始状态写成工厂函数，reset 时重新调用工厂。页面 reset 注册到统一数组，切账号或刷新时统一清理。

**必备模式 B：数据依赖层**

给共享数据起域名。写操作只声明哪些域变了，由统一表作废相关页面缓存，避免“改了 A 页，B 页还显示旧数据”。

**必备模式 C：桌面工作台滚动骨架**

桌面 WebView 不应让整个 document 承担滚动。下面是约束，不是必须原样复制的视觉样式：

```css
html,
body {
  width: 100%;
  height: 100%;
  margin: 0;
  overflow: hidden;
  overscroll-behavior: none;
}

.app-shell {
  width: 100%;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  display: grid;
  grid-template-columns: 248px minmax(0, 1fr);
  grid-template-rows: 60px minmax(0, 1fr);
}

.sidebar {
  min-width: 0;
  min-height: 0;
  overflow-y: auto;
  overscroll-behavior: contain;
}

.main-stage {
  min-width: 0;
  min-height: 0;
  overflow-x: hidden;
  overflow-y: auto;
  overscroll-behavior: contain;
  scrollbar-gutter: stable;
}
```

`minmax(0, 1fr)` 和子项 `min-height: 0` 不能省。Grid/Flex 子项默认最小尺寸可能等于内容高度，省掉后看似给了 `.main-stage` 滚动，实际仍会把根页面撑高。验收时应在顶部和底部继续滚动，确认 `document.scrollingElement.scrollTop` 始终为 0，侧栏和顶栏位置不变。

**必备模式 D：后台任务状态机**

长任务使用 `start -> poll/event -> terminal`，至少包含 `queued / running / succeeded / failed / cancelled`。每个任务记录阶段、进度、短错误、详细日志位置和是否可重试；关闭弹窗必须向后端发取消，而不是只隐藏 DOM。客户端重启后，不能把上次遗留的 `running` 永久显示为仍在工作，应通过租约过期或恢复逻辑收敛到可解释状态。

**必备模式 E：按通道单例与已有窗口唤醒**

单例使用操作系统持有的非阻塞文件锁：进程正常退出或崩溃后由系统释放，磁盘上即使留下锁文件也不代表被占用。不要只写 PID 文件，PID 会复用且崩溃后会残留；也不要拿业务 HTTP 固定端口代替单例锁，端口可能被无关软件占用，正式版与测试版也可能需要同时运行。

首个进程持锁后，在回环地址创建随机端口和随机口令，把最小唤醒信息写进当前通道私有目录。第二个进程拿锁失败后只发送一次带口令的 `activate`，随后退出；首个进程收到后调用桌面壳的 `restore + show/focus`。锁是正确性的真源，回环通道只是改善体验的辅助能力。锁目录、唤醒元数据、端口和口令都必须按通道隔离，退出时清理元数据，并测试崩溃/启动失败后锁会释放。

**【RedBeacon 实例】**

- 桌面入口是 PyInstaller + pywebview，核心仍是同一个 FastAPI + `static/index.html`。
- macOS 外壳实际使用 Cocoa `WKWebView`；Windows/Linux 使用 pywebview 对应的系统 WebView 后端。UI 渲染引擎和业务自动化浏览器是两回事：前者画工作台，Playwright/CloakBrowser 只承担卡片渲染、扫码和发布。
- `server.py` 默认绑定 `127.0.0.1:17771`，端口占用时退到随机端口。
- `services/single_instance.py` 使用 macOS/Linux `flock` 与 Windows `msvcrt.locking` 持有当前通道桌面锁；正式版位于 `~/.redbeacon/runtime/`，测试版位于 `~/.redbeacon_test/runtime/`。第二次启动只向首个进程发送带随机口令的本机唤醒请求，由 pywebview `restore()` + `show()` 恢复并置前已有窗口。
- `create_app(ui_token=...)` 有本机操作台口令保护。
- 前端已使用 `DIRTY_MAP` 管理选题、审核、发布、方案等缓存作废。
- 客户端更新已经走后台 job + poll，能展示进度。
- 根页面和应用外壳不滚动，主内容区拥有业务滚动；这同时避免 macOS WKWebView 橡皮筋回弹把整个工作台拖动。
- RedBeacon 的创作链路把产品名当作数字员工名：用户“交给 RedBeacon”后，工作卡按真实步骤显示写稿、画图、排版、入审稿台；发布链路继续显示准备素材、确认登录、帮用户发布和归档，不再用笼统的“生成中”。

---

## 8. Skill：真源一处，多宿主派生

**【原则】**

- skill 正文应是“人话指令 + 调同一套 CLI/接口”，不复制业务逻辑。
- skill 真源只能有一处，其他宿主格式从真源派生。
- 正式版和测试版 skill 必须分开，避免测试命令误驱动正式数据。
- 面向用户时少说 skill 名和命令名，除非用户主动要命令。

**【RedBeacon 实例】**

- `.claude/commands/redbeacon*.md` 是 stable skill 真源目录，名字是历史遗留，不代表只能用 Claude Code 维护。
- Codex skill 由 updater / `tools/sync-codex-skills.py` 从真源派生。
- 测试版由 `tools/build_channel_skills.py --channel test` 生成 `redbeacon-test*.md`，正文也改成调用 `redbeacon-test`。
- 正式/测试 skill 都进入当前应用 release unit 的 `skill/redbeacon-skill.tar.gz`，由全局发布 Skill 按 channel 放入不同中央版本树。

---

## 9. 打包、发布、更新、卸载

**【原则】**

数字员工的交付要按“普通软件”设计，而不是按开发者工具设计：

- **用户环境默认极不可信。** 不要假设用户装了 Python、Node、浏览器、字体、证书、命令行工具，也不要假设 PATH、HOME、LOCALAPPDATA、PowerShell 编码、执行策略、代理、杀毒软件、系统浏览器状态是正常的。安装包和安装脚本要把运行所需环境准备好。
- 主交付物是**自包含桌面包**：内置 Python 和依赖，用户不用装 Python、uv、pip。
- 官网安装必须采用“固定公网入口 + 动态选择器 + 不可变制品”三层模型：
  1. **固定公网入口**：官网页面、用户复制的一行命令和其中的脚本 URL 长期不变，正式版与测试版使用不同固定路径。
  2. **动态选择器**：平台入口按通道解析固定 canonical `latest.json`，严格校验项目、通道、版本、文件大小、SHA-256 和不可变 URL，再以 `307` 和 `Cache-Control: no-store` 跳到当前版本脚本或直接下载包；允许很短且有明确上限的服务端清单缓存（RedBeacon 当前 15 秒），但浏览器/CDN 不能缓存选择结果。清单异常时返回 503，不能猜文件名或悄悄退回历史源。
  3. **不可变制品**：被选中的版本化安装脚本再次读取同一通道 canonical manifest，精确选择并校验该 release unit 内的客户端、CLI、skill 和运行时。所有版本对象只新增、不覆盖。
- “官网脚本链接不变”不等于“脚本字节永远不变”：不变的是用户看到的公网 URL，变化的是选择器从最新清单选中的版本化脚本。准备或上传新制品不会影响现有用户；只有发布系统原子切换 canonical manifest 后，同一个官网 URL 才会在选择器短缓存到期内自动开始选择新版本。
- 普通版本发布不得修改官网页面、安装命令、数据库记录或 Nginx。只有产品 code、支持平台或 manifest/制品路径契约发生兼容性变化时，才需要平台与客户端协同改固定入口；这种变化必须先走测试版并保留旧入口兼容期。
- 构建节点可以是本机、受控虚拟机或 CI，但不能成为正式对外发布源。不要把用户安装或更新指到 GitHub Release、GitHub Raw、构建机共享目录或临时服务器。
- 项目构建与公开发布必须分权。项目只产出已测试、非隐藏、无链接、无凭据的干净制品树；不包含 OSS SDK/密钥、上传、节点 SSH、manifest 切换或别名发布逻辑。中央发布 Skill 独占上传与切流权，公开结果以其校验过的 `publication-result.json` 为唯一事实源。
- 安装脚本重复执行时，先拉小 manifest 判断本地版本；已是最新则跳过大包下载，避免浪费流量，但仍要刷新/修复 skill 和校验运行时依赖。重复安装既是更新入口，也是缺文件后的修复入口。
- 大型运行时依赖必须**安装期预热**：浏览器内核、OCR/渲染引擎、模型运行时、驱动、字体等，只要首次业务动作会用到，就不能留到用户第一次点击时才临时下载。宁可安装慢一点，也不要让用户在首个关键动作里无进度卡住。
- 运行期仍要能**自检和自修复**：安装成功不代表缓存永远存在。用户可能清理缓存、换启动方式、被杀毒软件隔离文件，客户端入口和 CLI 入口都要能检测缺失依赖，显示进度并补齐。
- 外部大依赖要进入同一中央 release unit，不能只依赖官方 CDN、GitHub、npm/pip 镜像或用户系统自带版本。客户端只从 canonical manifest 消费 `download_urls=[node_url,url]`：对节点只发一个不跟随跳转的 `Range: bytes=0-` GET，连接 3 秒、首响应/首字节 8 秒、后续读取停滞 15 秒；任何 HTTP/TLS/超时/中断/Content-Range/大小/SHA 异常都删除节点半包，直接回落不可变中央 OSS `url`。
- 下载节点域名属于**发布侧身份绑定**，不是客户端配置。ByteStaff 当前 active origin 是 `https://download.bytestaff.jiomig.com`，新接入或已迁移项目的回执与受保护 Profile 必须逐字一致。更换节点域名时，先确认全部旧发布事务已终态并验证新域名的健康、就绪和真实制品 Range，再签发新项目回执并把 `node_base_url`、受保护 Profile `base_url`、`receipt_id` 和回执原始 SHA 原子同步；intake、OSS origin、SSH 身份和历史 manifest 没有变化就保持原样。客户端业务代码不改域名，只读取 manifest 的 `download_urls`；旧版本 `node_url` 不回写，新版本才使用新 origin，OSS `url` 始终保留为最终回退。
- 几十 MB 以上的不可变资产必须支持**分段、续传、停滞切源**：优先使用 2-4 个受控 Range 连接提高吞吐，保留已完成分段，重试或重开安装器时从断点继续；连接建立后长时间无数据也要按停滞处理。UI/终端必须持续显示节点、百分比、已下载大小和速度，不能只显示一个长期不动的 0%。
- 下载清单要按业务真实依赖最小化。第三方安装命令默认拉取的附加浏览器、headless shell、示例模型、调试资产等，如果业务入口从不使用，就不应让每位用户承担其带宽、磁盘和失败概率；裁剪后必须用真实最小业务动作验证没有误删。
- 依赖缓存必须**归属当前产品和通道**：正式版、测试版、开发版各用自己的缓存目录，不要使用库默认的全局缓存，也不要相信用户已有浏览器或系统安装包。
- 卸载也要遵守所有权边界：只清当前产品、当前通道拥有的依赖缓存，不能为了“清干净”去删系统全局缓存或其他产品可能共用的目录。
- 每个入口都必须**主动设置运行环境**：安装脚本、CLI 初始化、UI 后端、后台线程、子进程渲染器、自动化 worker 都要显式设置依赖目录和必要环境变量。不能只在安装脚本里设置一次，然后假设客户端双击、AI 助手、PowerShell、cmd、系统计划任务都会继承同一份环境。
- 自包含冻结包应在业务模块导入前运行最小 runtime hook，清掉外部继承的 channel/build-channel、数据目录、日志目录、共享令牌目录、浏览器缓存和第三方二进制路径等覆盖值，再用**包内构建通道强制赋值**计算产品固定目录。不能用 `setdefault`：旧 PowerShell 留下的 `test` 足以让正式包读到测试目录，看起来像更新后账号全丢。开发态的测试覆盖开关不能原样成为用户版运行时的隐式配置入口，测试版安装包装脚本结束后还要恢复调用者原环境。
- 第三方库的默认行为必须被产品接管：如果库会读 `PATH`、`HOME`、`LOCALAPPDATA`、默认缓存目录、用户自定义二进制路径、版本 pin、自动更新开关等，产品要么显式覆盖，要么明确清掉危险变量，避免用户机器上的历史配置污染运行。
- 版本化运行时必须校验“精确版本 + 真启动”，不能扫描到任意旧缓存就宣告就绪。浏览器、驱动、模型等只要上层 SDK 固定 revision，就必须按该 revision 找可执行文件，并在安装结束前完成一次最小离线工作；旧缓存只能用于诊断，不能用于兜底冒充。
- 架构兼容要按**目标平台**判断，不按当前构建机推导。macOS 构建机不能决定 Windows 包扩展名；Windows ARM64 也不能因为第三方库只列 `AMD64` 就直接判死。没有原生包时，要明确策略：使用 x64 包走系统仿真，或给用户清晰提示“不支持且不可修复”。
- “可控依赖全包”不等于“不需要任何系统条件”。仍然要承认并检测最小前提：能联网访问分发源、能写当前用户目录、系统允许启动本地进程、磁盘空间足够。前提不满足时给人话错误和下一步，不给堆栈。
- 更新入口必须语义一致：客户端点击更新、CLI update、AI 助手触发更新，都应该做同一通道的全量更新。
- 全量更新包括：客户端整包、CLI 兼容入口、skill 和与当前 SDK 精确配套的外部运行时。不要只更新某一个片段，也不要允许这些片段来自不同版本。
- 正在运行的客户端不能原地覆盖：先下载、校验、解压到 staging，再由外部 updater 等进程退出后替换安装目录。
- 更新时必须把旧安装和继承来的环境变量都视为不可信输入，而不是可复用环境：在 staging 里用新版本程序、当前通道固定目录准备并真实启动验证新版本所需的外部运行时、驱动和 skill，全部成功后才关闭旧进程。`setup` 与安装健康检查必须是数据库无关的；桌面 smoke 使用临时空库，绝不能为了验新包而提前迁移用户真实库。停止旧进程后先保存 SQLite 主库及 WAL/SHM 快照，再把快照复制到临时目录，用新版本在副本上演练迁移和桌面初始化；通过后才替换客户端。旧客户端与当前通道 skill 都保留回滚副本，新版本放置后再次运行临时库桌面初始化和至少一个真实辅助任务，最后才提交；任一步失败都恢复整套旧版本，用户数据库保持原样，不能留下“新程序 + 旧依赖”或“新程序 + 旧 skill”的半更新状态。
- 卸载默认保留业务数据和登录令牌；只有显式 purge 且值属于 `1/true/yes/on` 才清用户数据。环境变量存在但值为 `0`、`false` 或空不能被当成授权删除。
- 卸载要清理可重建运行时缓存：业务数据默认保留，但浏览器内核、临时解压目录、旧版本缓存、快捷方式、启动项、skill 等可重建内容要按通道清理干净。
- 发布前必须有测试版缓冲通道。流程必须是：先发测试版 -> 用户人工测试确认 -> 同一套代码、同一打包流程切正式通道 -> 再发正式版。
- 构建依赖必须整棵锁定。只锁直接依赖仍不够，间接依赖也会漂移；必须提交完整锁文件并让每个构建节点以 frozen 模式安装，同时固定构建工具和解释器 patch 版本。否则测试版和正式版即使来自同一提交，也可能因为两次构建解析到不同版本而表现不同。
- 发布包、skill 和运行时都使用带版本号的不可变路径并校验 SHA-256。中央发布先 prepare 不可变双源对象，保留旧 canonical；真实客户端完成节点正常、节点失败回落、坏节点文件回落和旧 `url` 单源兼容验收后，才用同一 run 的 prepare receipt 原子切换 canonical。

### 9.1 本地双主机构建范式：Mac 主控 + Windows 虚拟机

当团队有一台性能充足的 Apple Silicon Mac，并能长期控制一台真实 Windows 11 虚拟机时，可以不用云 CI 完成 Windows/macOS 桌面包构建。它不是“在 Mac 上交叉编译 Windows”，而是由 Mac 负责编排，两个目标系统各自在原生 OS 中打包：

```text
Apple Silicon Mac（唯一编排端）
├── 创建一次已提交源码归档并计算 SHA-256
├── 在全新临时目录构建、测试 macOS arm64
├── 通过 SSH/SCP 把同一归档送进 Windows
├── 收回 Windows 成品并组装干净 release-artifacts
└── 两端都成功后写本地 build-evidence.json
        │
        └── SSH 密钥连接
            Windows 11 ARM64 虚拟机
            ├── 在系统 x64 仿真下运行 x64 CPython
            ├── 用 x64 Python + PyInstaller 产出 AMD64 程序
            └── 在真实 Windows 中做冻结包和安装事务 smoke
```

这套方案必须保留 CI 原本承担的纪律，不能退化成“Mac 点一次、Windows 再手工点一次”：

1. **构建输入只能是一份干净提交。** 编排器先拒绝有未提交改动的业务仓，再对唯一 commit 做一次 `git archive`。Mac 和 Windows 都只解压这份归档，不能各自在活动工作区读取源码。
2. **工具链与依赖必须固定。** Python patch、uv、PyInstaller、直接依赖和完整锁文件都进入构建契约；两端都用 frozen 模式重建环境，并强制重装当前项目元数据，随后断言安装元数据版本等于源码运行时版本。
3. **Windows ARM64 构建 x64 包靠 x64 解释器，不靠改文件名。** Windows 虚拟机显式安装并运行 x64 CPython；PyInstaller 产物跟随解释器架构。必须读取 Python、GUI、CLI、Renderer 的 PE machine，全部为 `0x8664` 才能称作 `win-x64`。宿主 `platform.machine()` 报告 ARM64 不影响这个结论。
4. **虚拟机只接受受控 SSH。** 使用密钥和非交互 `BatchMode`；Windows 防火墙的 22 端口只允许 Mac 宿主地址；长构建期间关闭自动休眠。不要把 SSH 密码、OSS 密钥或平台生产密钥写进仓库。项目构建环境不应拥有 OSS 凭证，Windows 只负责产出并回传包。
5. **每个平台都先验源码，再验冻结包。** 两端分别跑全量测试。macOS 还要核对 CPU 架构、`CFBundleExecutable`、GUI 真实启动、CLI、浏览器和 Renderer 产图；Windows 还要核对三份 AMD64 PE、GUI/CLI 启动日志、PowerShell 5.1 解析、浏览器真实启动、Renderer 产图，以及正式/测试安装、覆盖、回滚、卸载事务。
6. **两端成功之前不产出可发布制品树。** 可以在本地保留单平台成品；只有 Mac 与 Windows 都通过后，才组装完整 `release-artifacts/` 和 `build-evidence.json`。
7. **构建和发布分成两个权限边界。** 构建只准备干净制品树，不网络发布。全局发布 Skill 从已提交项目契约、受保护 profile 和干净制品树重新建立 provenance，准备不可变双源对象，经真实客户端验收后才切 canonical。
8. **测试版和正式版复用同一编排器。** 正式构建必须有显式人工批准开关。测试通过后不能改代码、锁文件、spec、安装脚本、skill 或发布脚本；只允许 channel 改变产品名、bundle id、数据目录、命令、skill 和 OSS 路径。任何相关改动都会让测试结论失效，必须重发测试版。
9. **旧云构建不能暗中并行。** 本地方案成为真源后，应删除或禁用活动目录中的自动构建 workflow，避免重复费用和双轨产物。历史方案只能冷归档，不能继续被默认上下文和后续 agent 当作现行流程。

`build-evidence.json` 至少记录：channel、version、业务 commit、源码归档 SHA-256、安装源归档 SHA-256、固定 Python/uv 版本、精确平台列表和 UTC 时间。它是“这两个包属于同一批构建”的项目侧凭据，不是发布成功证明。

推荐的发布阻断矩阵：

| 阶段 | macOS arm64 | Windows x64 | 任一失败时 |
| --- | --- | --- | --- |
| 源码与依赖 | 干净 commit、同源归档、frozen 安装、版本元数据一致 | 同左，并确认 x64 Python | 停止，不产出完成标记 |
| 回归测试 | 当前完整测试集 | 当前完整测试集 | 停止，不上传 |
| 冻结程序 | GUI、CLI、辅助进程、包内资源、`Info.plist` | GUI、CLI、辅助进程、三份 `0x8664` PE | 停止，不上传 |
| 真实最小动作 | 桌面初始化、两套浏览器启动、真实产图 | 同左，加 PowerShell 解析与安装事务 | 停止，不上传 |
| 分发准备 | 干净制品树 + SHA-256 | 同一制品树 + SHA-256 | 不写 `build-evidence.json` |
| 发布切流 | 真实客户端双源验收 | 与 macOS 同一 canonical | 全局 Skill 不切流，线上保持旧版 |

**【RedBeacon 实例】**

当前发布事实：

- canonical manifest 源：中央上海 OSS `https://bytestaff-download-releases.oss-cn-shanghai.aliyuncs.com/projects/redbeacon/<channel>/latest.json`。
- `tools/build_desktop_local.sh`：从一个已提交 CLI `git archive` 顺序完成 Apple Silicon Mac 本机 `mac-arm64` 与 SSH Windows 11 ARM64 虚拟机 x64 Python `win-x64` 构建，再组装客户端、安装器、skill、Playwright/CloakBrowser 和 provenance 的干净制品树；它不拥有任何发布凭据。
- 公开发布唯一入口：全局 `bytestaff-digital-employee-publish` Skill；项目内无 OSS 上传、节点上传、manifest 生成/切换或固定别名发布脚本。
- 每个平台低级错误由真实系统 smoke 阻断：先跑全量回归，再检查冻结包 `--version`、桌面最小启动、浏览器预热和真实渲染；Windows 另加 x64 PE、PowerShell 解析与安装事务扫描。
- 测试版和正式版客户端打包走同一本地编排器和同一 PyInstaller spec；只允许 channel 造成名字、bundle id、数据目录、canonical 路径和 skill 名不同。
- 同一份 PyInstaller Analysis 收集依赖，产出无控制台 GUI、可见输出 CLI 和独立 `RedBeaconRenderer`；统一 dispatcher 根据入口/参数进入桌面或命令行。macOS bundle 显式把 `CFBundleExecutable` 指向 GUI，不能让辅助渲染器成为 Finder 启动目标。
- 多平台构建不能只靠“文件存在”判断完成。所有当前支持平台成功后才生成带版本、通道、commit、同源归档 SHA、工具链版本和平台列表的 `build-evidence.json`；全局 Skill 会再以仓库契约和 provenance 独立核对。
- 更新清单：正式 `projects/redbeacon/stable/latest.json`，测试 `projects/redbeacon/test/latest.json`。
- 更新服务：`cli/src/redbeacon/services/updater.py`。
- 安装脚本：`install/install.sh`、`install/install.ps1`；测试入口只是设置 test 通道后复用主脚本。
- 卸载脚本：默认保留数据，`REDBEACON_PURGE=1` 才清数据。
- 浏览器运行时：安装和运行期都通过 `redbeacon setup` / browser engine 准备 Playwright Chromium 与 CloakBrowser Chromium，缓存固定在 RedBeacon 通道目录。两套内核作为版本化制品进入中央 manifest，客户端先试一次统一节点 Range GET，失败或校验不符后回落中央 OSS。Playwright 只下载业务使用的完整 Chromium，不附带未使用的 headless shell。项目构建负责准备当前 Windows/macOS 资产，全局发布 Skill 负责不可变双源上传与验证。
- 关键动作兜底：扫码登录 / 发布优先使用 CloakBrowser，但如果第三方内核下载成功后仍在启动阶段关闭，自动切到 RedBeacon 自己预热的 Playwright Chromium，并使用通道内独立 fallback profile。大依赖必须验证“能启动并创建页面”，不能把“下载完成”当作“可用”。
- 扫码会话语义：用户每次点扫码，都要先停止旧会话，再创建新会话，不能复用用户可能已经手动关闭的浏览器窗口。用户点“重新扫码/重新登录”时，还必须删除当前通道的浏览器 profile 和 cookie 文件。只删外部 cookie 标记不够，因为浏览器 profile 里还有持久化登录态、锁文件和坏上下文状态。用户关闭扫码弹窗也必须通知后端取消等待并关闭浏览器，不能只把弹窗藏起来。
- 运行环境接管：`browser_engine.configure_runtime_environment()` 会固定 `PLAYWRIGHT_BROWSERS_PATH`、`CLOAKBROWSER_CACHE_DIR`，关闭 CloakBrowser 自动更新，清掉会污染运行的 `CLOAKBROWSER_BINARY_PATH` / `CLOAKBROWSER_VERSION` / `CLOAKBROWSER_SKIP_CHECKSUM`。CLI 初始化、扫码登录、发布会话、卡片渲染子进程都会调用同一套逻辑。
- 随包辅助进程也属于产品本体：卡片渲染器、OCR、转码器等只要业务会调用，就必须在 PyInstaller/安装包清单里显式打包，并在每个声明支持的平台冻结包 smoke 中分别验证“文件存在 + 进程能启动 + 真实最小产物”。只测主客户端能打开，无法证明完整业务可用。
- 组合能力必须逐项满足契约：例如“AI 封面 + 文字卡片”不是“至少有一张图就成功”，而是 AI 封面和文字卡片两项都要有结果。缺任一必选产物都应停止入库、保留可诊断日志并返回可操作提示，绝不能静默保存半成品。
- Windows ARM64：当前策略是正式 Windows 桌面包仍为 x64 包，Windows ARM64 机器通过系统 x64 仿真运行；CloakBrowser 平台表被映射到 `windows-x64` 内核包，避免扫码登录在第三方平台检测阶段失败。

现行固定动作如下，其他数字员工可以替换项目名和路径，但不应删减其中的阻断关系：

```bash
# 1. 先构建并验证干净测试版制品树，不做网络发布
tools/build_desktop_local.sh --channel test

# 2. 在项目根目录调用全局 bytestaff-digital-employee-publish Skill；
#    prepare 后先做四项真实客户端验收，再恢复同一 run 切 test canonical

# 3. 用户明确确认测试通过，且期间没有任何相关改动后，重走同一构建器
REDBEACON_STABLE_APPROVED=1 tools/build_desktop_local.sh --channel stable
#    随后仍由同一全局 Skill 携带独立 stable 批准凭证发布
```

Windows 虚拟机只需一次性以管理员运行初始化脚本，安装 OpenSSH Server、限定宿主机来源、写入公钥、固定 x64 uv 并关闭交流电休眠；日常发布不再远程操作桌面，只由主控脚本走 SSH。RedBeacon 首次正式使用这条新链路时，构建器在切 manifest 前拦住了 PowerShell 传递 Python 内联代码时的引号丢失问题；修复为两端共用的独立版本校验脚本后，完整重跑 Mac/Windows 测试和冻结包 smoke 才允许发布。这证明发布门禁必须以真实目标系统结果为准，不能以“脚本看起来正确”为准。

旧的“先装 uv，再 uv tool install wheel，再本机生成桌面图标”只能作为历史兼容经验，不能再作为新项目母本主路径。

---

## 10. 测试版缓冲通道：发布前的安全阀

**【原则】**

每个数字员工都应该有正式版、测试版、开发版三层隔离。测试版不是“同一个包换个下载地址”，而是可以和正式版同时存在的独立产品。

测试版至少隔离：

- 应用名 / bundle id / 签名标识。
- CLI 命令名。
- 本机业务数据目录。
- 平台令牌目录。
- skill 名和安装目录。
- canonical manifest 路径和中央版本树。
- 桌面快捷方式 / 启动台图标。

测试通过后再发布正式通道。不要让未验证版本直接覆盖所有用户。测试版发布后如果又改了任何客户端、CLI、skill、安装/更新/卸载或发布脚本，必须重新发测试版；不能把改过的代码直接发正式版。

**【RedBeacon 实例】**

| 项目 | 正式版 | 测试版 |
|---|---|---|
| 应用名 | `RedBeacon` | `RedBeacon_test` |
| 命令 | `redbeacon` | `redbeacon-test` |
| 打包内 CLI | `redbeacon-cli` | `redbeacon-test-cli` |
| 业务数据 | `~/.redbeacon` | `~/.redbeacon_test` |
| 平台 token | `~/.bytestaff` | `~/.bytestaff_test` |
| manifest | `projects/redbeacon/stable/latest.json` | `projects/redbeacon/test/latest.json` |
| 客户端版本包 | `projects/redbeacon/stable/releases/<version>/packages/RedBeacon-*.zip` | `projects/redbeacon/test/releases/<version>/packages/RedBeacon_test-*.zip` |
| skill 版本包 | `.../stable/releases/<version>/skill/redbeacon-skill.tar.gz` | `.../test/releases/<version>/skill/redbeacon-skill.tar.gz` |
| Codex 扫描目录 | `~/.codex/skills/redbeacon*/SKILL.md` | `~/.codex/skills/redbeacon-test*/SKILL.md` |
| 浏览器/profile | `~/.redbeacon/browser/` 等正式目录 | `~/.redbeacon_test/browser/` 等测试目录 |

测试版验证方法见 `RedBeacon-测试版验证指南.md`。

---

## 11. 安全、稳定、可维护性底线

**【原则】**

- **密钥不上云、不进日志。** 平台 token、第三方 cookie、用户云表密钥只落本机隐私目录。
- **客户端不做值钱权限判断。** 本地“已解锁”标志不可信，鉴权和扣点必须现查服务端。
- **用户电脑不是开发机。** 所有入口都要按最差客户机设计：无开发工具、路径有空格/中文、权限受限、系统浏览器不可用、控制台编码不稳定、网络被代理或拦截、杀毒软件可能隔离新文件。能在安装期解决的都不要推给用户。
- **网络失败不拖垮本地使用。** 版本检查、看板刷新、可选外部状态要有短超时和缓存；主动发起的扣点/写入动作要明确失败。
- **错误要能自愈。** CLI 失败用结构化错误和下一步；UI 给用户人话，不给堆栈。
- **机器输出永远是不可信输入。** `response_format=json` / schema 模式只能降低出错率，不能取代平台契约验收、客户端领域规范化和副作用前重验。
- **先按数据链路分层，再按操作系统归因。** 问题出现在 Windows 不代表根因是 GBK 或 PowerShell。先比对上游原文、HTTP JSON、解析后字段、数据库值、UI 显示和渲染输入；“重试第二次正常”通常只证明上游输出有随机性，不证明问题已修复。
- **长等待必须有进度。** 下载、解压、浏览器启动、扫码登录、发布、更新都要能显示当前阶段。只显示“正在处理”超过几十秒，用户会按“软件坏了”处理。
- **不要信任 inherited environment。** 从桌面双击、终端、AI 助手、安装脚本、后台 updater 启动，同一个程序拿到的环境变量可能完全不同。关键路径必须在进程内重新设置依赖目录和运行参数。
- **不要信任第三方库的平台表。** 依赖库常常只覆盖主流 CPU/OS 文案，但真实用户会有 Windows ARM64、中文用户名、OneDrive 桌面、企业代理、受控文件夹访问。产品要在自己的兼容层里处理这些差异。
- **跨平台脚本要防低级错误。** Windows PowerShell 尤其要做解析和假安装冒烟；输出尽量 ASCII，避免客户机乱码。
- **发布前必须验“第一步能打开”。** 不要求自动化覆盖所有业务，但要排除解压、exe 缺失、版本命令、启动入口、日志 stdout/stderr 这类低级崩溃；每个对外宣称支持的平台都要在真实系统里跑冻结包 smoke，不能用一个系统替另一个系统背书。
- **运行时依赖要有契约检查。** 发布前不仅检查代码能跑，还要检查安装脚本会预热依赖、OSS 上存在当前版本所有支持平台大包、缓存目录按通道隔离、运行期入口能自修复。
- **平台限额按账号共享。** 当服务端按账号聚合并发/频率时，客户端的窗口、CLI、skill 和多个进程必须共用本地租约与令牌桶；只在单进程里建 Semaphore 会在真实用户多入口使用时失效。动态限额从 checkin 读取，缺字段才用保守默认。
- **批量业务默认排队，不默认并发。** 用户一次选择多条内容，不等于客户端可以同时启动多个重任务。生成、渲染、上传这类高成本链路应进入有上限、可见进度的 FIFO 队列，一条完整结束后再启动下一条；只有经过容量评估且确有收益的轻任务才允许受控并发。RedBeacon 的所有笔记生成入口共用一个串行工位，待发任务最多 20 个。
- **幂等和重试必须是一套状态机。** 限流与处理中沿用原 request_id；明确失败退款才换新 id；网络断线与 503 结果未知只能确认，不能并行重放。重试次数、租约和短熔断应落共享本地状态，进程退出后不能无限清零。
- **旧方案要归档，不要混在主文档里。** 历史文档可以留，但 AGENTS/母本/README 必须只讲当前事实。

**【RedBeacon 实例】**

当前已具备：本机 UI token、OSS sha256 校验、Windows 安装冒烟、Windows bundle smoke、测试版隔离、全量更新、卸载保留数据、浏览器内核安装期预热、扫码/发布浏览器内核运行期自修复、OSS 浏览器内核兜底镜像。

当前仍需长期注意：代码注释里还有旧飞书主流程措辞，后续维护时应逐步清理，避免新功能照着旧注释做判断。

---

## 12. 开新数字员工的落地清单

1. 定义这个员工包哪条专业工作流，压缩成 3 到 8 个核心用例。
2. 先搭 `core/domain`、`core/ports`、`core/usecases`、`core/presets`、`infra`、`composition`。
3. 用桩件把核心用例离线跑通，再接平台、浏览器、云表、本地库。
4. 明确数据真源：默认本机还是默认云端，只能选一个主路径；另一个作为可选源时必须通过 composition 二选一注入。
5. 接平台 device flow、checkin、AI 能力调用；验证未登录、点数不足、平台不可达时 fail-closed。
6. 做 UI 主入口：本机后端 + 单个前端入口 + 页面 reset + 数据依赖作废 + 长任务进度。
7. 做 skill：真源一处，派生到不同 AI 宿主；读能力给 AI “眼睛”，高密度写操作交给 UI。
8. 列出所有运行时依赖：浏览器内核、字体、渲染器、驱动、外部 CLI、模型文件、证书等；决定哪些内置、哪些安装期下载、哪些运行期自修复。
9. 做安装/卸载/更新：OSS 分发、自包含包、小 manifest 跳过重复下载、全量更新、默认保留数据、可重建运行时缓存按通道清理。
10. 做测试版通道：名字、命令、数据、token、skill、manifest、包路径、运行时缓存全隔离。
11. 做运行环境接管层：每个入口启动时都固定数据目录、缓存目录、二进制路径、下载源、自动更新策略、危险环境变量清理；后台线程和子进程也必须继承同一套设置。
12. 做平台/架构兼容矩阵：Windows x64、Windows ARM64、macOS arm64、Linux x64 至少要有明确策略；没有原生依赖包的平台必须有替代路径或清晰拒绝。没有真实构建与 smoke 的平台不得出现在下载页。
13. 做原生系统冒烟：每个支持平台的包能解压、能跑版本命令、客户端能启动到第一步；Windows PowerShell 还要解析和完整假安装。构建节点可以是本机/虚拟机/CI，但目标程序必须在目标 OS 中用目标架构解释器构建。
14. 做发布契约检查：项目打包只产出干净制品树和构建证据，不携带 OSS 凭据、节点 SSH、上传或切流逻辑；全局发布 Skill 校验 active 回执/Profile 使用当前官方节点 origin，生成 `download_urls=[node_url,oss_url]`，并在切 canonical 前验证安装会预热运行时依赖、当前版本全部支持平台大包和双源回退。节点换域名必须终态审计、重签回执和原子迁移 Profile，历史 manifest 不回写。
15. 发布前先发测试版；人工确认安装、启动、更新、卸载、skill 隔离和首个关键业务动作后，不改代码，用同一打包流程切正式通道再发正式版。
16. 把旧方案归档，把当前事实写进 `AGENTS.md` / README / 发布指南，避免后续 agent 被历史文档带偏。
17. 为每种机器可解析交付物定义白名单输出契约、必需字段和“不可恢复失败 / 可恢复表示偏差”表；实装结算、入站、副作用三层闸门。
18. 给规范化器做边界测试：正常结果、原始换行、多转义一层、未转义引号、缺字段、空字段、中文/emoji、Windows 路径、正则和代码片段；验证连续规范化两次不会继续改变数据。
19. 对预览/提交、导入/恢复、本地队列重放和旧版数据迁移分别做绕过测试，确认任何路径都不能跳过副作用前的业务不变量。
20. 建数据库生命周期清单：每张业务表都必须覆盖迁移、账号级联删除、备份、恢复、purge 和完整性测试；用真实 round-trip 对账，不靠手写 counts 猜覆盖率。
21. 建桌面 UI 契约：根页面不滚动、主内容独立滚动、长任务可取消、错误有尺寸上限、首屏资源全部本地、同一通道只运行一个客户端；用桌面/移动尺寸和 WebView 实际滚动边界验证，并测试第二次启动只唤醒已有窗口，正式版与测试版仍可并存。
22. 建冻结包清单：GUI、CLI、Renderer/辅助进程、动态模块、包内资源、Info.plist/Windows 资源、标准流和真实最小产物都逐项 smoke。
23. 明确平台支持和签名状态：支持哪种 OS/CPU、哪种走仿真、哪种未覆盖、是否已有 macOS 公证和 Windows Authenticode；不要把计划写成已完成能力。
24. 给每个历史 P1/P2 故障绑定防复发测试，并把测试放进发布阻断路径；只有说明文档、没有自动化约束的修复不算真正沉淀。

---

## 13. 故障图谱：踩过的坑与防复发规则

本节按“症状 -> 根因 -> 固化规则”记录。它不是故事集，而是新项目的测试来源：每修过一种问题，都应至少留下一个单元测试、契约检查、安装 smoke 或对应平台冻结包 smoke。

### 13.1 架构、数据与文档

- **双写数据必漂移。** 本地一份、云端一份再互相同步，最终会虚报、覆盖和丢状态。一个业务对象同一时刻只允许一个真源，切换通过 composition 注入。
- **UI 寄生 CLI 会失控。** UI 拼命令、解析人类可读文本，短期快，长期任何文案调整都会变成接口破坏。UI 和 CLI 应并列调用 core，用结构化领域结果各自渲染。
- **模块 import 就初始化数据库，会让只读命令也崩。** `--help`、`--version`、`update --check` 不该因为数据目录只读而失败。数据库和迁移应在真正需要业务数据的用例边界延迟初始化。
- **冻结包用 `setdefault` 写通道，会把账号“变没”。** 测试版安装命令可能在当前 PowerShell 留下 `REDBEACON_CHANNEL=test`；正式包若只在变量不存在时写 stable，就会打开测试目录或新建空库。冻结包先清外部 channel，再强制写自身构建通道；正式/测试包装脚本必须恢复调用者环境，并用敌对环境变量做回归。
- **拿用户真实库做安装 smoke，会在备份前迁移数据。** 新包尚未提交时运行桌面初始化，可能已经执行 SQLite migration；即使客户端随后回滚，数据库也回不去了。`setup` 保持数据库无关，普通 smoke 用临时空库；旧库兼容只在停止旧进程后的快照副本上验证，原库直到用户真正启动新版本前都不写。
- **把“环境变量非空”当 purge，会让 `PURGE=0` 也删库。** 删除用户数据属于明确授权，只接受有限真值集合；安装事务 smoke 要放入账号哨兵数据，验证成功更新、失败回滚和普通卸载后字节仍一致，并确认存在可恢复快照。
- **账号删除只删主表，会被外键直接拦住。** image strategy、模板、提示词、profile、选题、审核、归档等从表必须统一级联或按显式顺序删除，并把完整性错误转成人话结构化错误。
- **备份清单靠手写，新增表就会悄悄丢数据。** 数据库 schema、备份、恢复、迁移、purge 和级联删除要共享一份表注册或同一套元数据；每次 schema 变化都跑“写满 -> 备份 -> 清空 -> 恢复 -> 对账”测试。
- **新用户拿到空方案，会不知道成品应该是什么。** 初始数据不是随手占位符，应使用已经验证的经典通用模板，既能直接运行，也能成为用户修改的参照。
- **预设在多处各写一份，设置成功却预览仍用旧值。** 内置模板、用户覆盖、预览拼装和最终执行必须经过同一个 preset resolver；`set` 后的 preview 与真正提交要做一致性测试。
- **组合结果只验“至少成功一个”，会保存半成品。** “AI 封面 + 文字卡片”等组合能力必须逐项声明 required/optional；任一必需产物失败就阻止入库，不能因为已有一张图而吞掉渲染错误。
- **旧术语和旧链接会反向改坏新代码。** 飞书主流程、旧官网、旧下载源、旧 AI 宿主的文档若仍出现在 help、UI 或注释里，后续 agent 会把它当现状。入口文档只保留当前事实，历史资料移入带日期的归档并明确不可执行。

### 13.2 平台、扣点与 AI 交付物

- **员工来源标签不是权限。** `product` 适合归因和统计，不应用来判断公共 AI 能力；账号、钱包和平台能力配置才是权限真源。
- **客户端事后自报“生成失败”再退款，既不可靠也可被滥用。** 必需交付物的 schema 和验收规则要在调用前登记，由平台在结算前检查并原路回补；客户端只展示平台确认的结算结果。
- **合法 JSON 不等于可交付成品。** 可见 `\n`、空正文、错误枚举、缺图、伪文字都可能通过 JSON 语法校验。平台结构契约、客户端领域验收和副作用前重验缺一不可。
- **为了修格式再调一次 AI，会重复扣点。** 能无损还原的表示偏差在本机做字段级幂等修复；缺失必需内容的真失败交给平台结算闸门，不能用第二次收费请求修 JSON。
- **粗暴反转义会制造新损坏。** 全局 `unicode_escape` 或无条件替换 `\n` 会破坏 emoji、Windows 路径、代码和正则。只允许对白名单字段、已知单层偏差做最小变换，并验证执行两次结果不再变化。
- **预览正常不代表提交载荷仍正常。** 用户编辑、旧前端、队列延迟和客户端升级都可能改变 payload。渲染、入库、发布前必须重新构造领域对象并验全部不变量。
- **批量选择不等于并发调用。** 多窗口、CLI 和 skill 若各自用进程内 Semaphore，仍会把同一平台账号打爆。重任务使用账号级共享调度库和有上限 FIFO；RedBeacon 笔记生成严格逐篇完整结束后再开始下一篇。
- **重试没有 request_id 状态机，会重复扣费或永远卡住。** 429/处理中沿用原 id；明确失败并回补后换新 id；网络断线和 503 的未知结果只允许有界确认。每次等待前释放运行槽并遵守 Retry-After。
- **AI 图片能显示，不代表可以原样落盘。** 原图可能包含模型名、工作流、EXIF、XMP、ICC、注释或时间戳。先解码像素、应用方向和色彩转换，再写成无 ancillary chunk 的纯 PNG；净化失败就丢弃，绝不能回退保存原容器。
- **只在模板里提醒“简体中文”仍会漏历史自定义方案。** 语言规则既要进入所有内置视觉模板，也要在最终生图边界幂等补齐，并保持用户业务标题原样；无文字模式则明确禁止额外加字。

### 13.3 桌面 UI 与用户体验

- **让 body 滚动会触发 macOS 整窗回弹。** 即使侧栏 `position: fixed`，WKWebView 的原生橡皮筋仍可能拖动整幅画面。锁定根页面和应用壳，把主内容区设为唯一滚动所有者，并用 `minmax(0, 1fr)`、`min-height: 0` 和 `overscroll-behavior` 验收。
- **只加 `overscroll-behavior` 不一定有效。** 如果 Grid/Flex 子项仍按内容最小高度撑开根页面，document 还是会滚。必须同时检查布局轨道可收缩，并在浏览器测试中断言根 scrollTop 不变。
- **外部 CSS/JS 会把离线客户端变成网络页面。** 启动时任何 CDN、字体或远程脚本都可能拖慢、被劫持或在企业网络失效。首屏资源全部随包，本机 API 慢时先显示静态启动页和真实阶段。
- **原始异常可以冲出整个 App。** 自动化 traceback、平台响应和 subprocess stderr 要进详细日志；UI 只显示长度受限、可换行、可关闭的人话摘要和下一步，错误容器不得改变主布局尺寸或遮住全部操作。
- **一个无限转圈不是进度。** 下载、浏览器准备、写稿、画图、排版、发布和更新要显示真实阶段、百分比或已完成项；停滞超时后自动切源或给重试，不让用户猜软件是不是坏了。
- **关掉弹窗不等于取消任务。** 前端必须调用 cancel，后台 worker 必须响应取消、关闭浏览器/文件句柄并释放队列租约；否则下一次操作会遇到 profile 锁和死 context。
- **拟人化文案如果不对应真实状态，就是欺骗。** 排队、写稿、画图、排版、入审、发布、归档分别用员工名描述真实动作；排队时不能说已经在写，失败时不能用卖萌代替原因。
- **全局定时设置会让批量发布含义混乱。** 发布时间属于每篇笔记；默认立即，切换后选择时间，入队和真正提交时都重验平台允许窗口。过期计划必须拦截，不能静默降级成立即发布。
- **固定端口不等于客户端单例。** 端口被占用后退到随机端口，会让第二次启动顺利开出第二套服务和窗口；反过来，无关程序占住固定端口也会造成误判。先按“产品 + 通道”获取操作系统文件锁，再决定服务端口；拿锁失败只唤醒已有窗口并退出。
- **PID 文件不是可靠单例锁。** 客户端崩溃会留下文件，系统又会复用 PID，手工判断容易把新进程当旧进程。使用由内核持有、进程结束自动释放的锁；磁盘文件只承载锁和唤醒元数据，不能仅凭文件存在判定正在运行。

### 13.4 浏览器自动化与大型运行时

- **把浏览器下载留到第一次扫码，会被理解为登录坏了。** Playwright、CloakBrowser、OCR、模型等关键大依赖必须安装期预热；运行期缺失时才走带进度的修复入口。
- **官方 CDN 不是可控运行环境。** 中国大陆用户的速度和可达性会随机变化。把精确版本的各支持平台资产同步到自有大陆 OSS/CDN并排第一，公共镜像和官方源只兜底。
- **用 HEAD 预检下载源会误杀可用节点。** 有些镜像 HEAD 异常但 GET 正常。直接做真实 Range GET，支持 2-4 段并发、断点续传、速度显示和无数据停滞切源。
- **下载成功不等于运行成功。** 压缩包可能损坏、CPU 映射错误、动态库缺失或浏览器启动即退出。安装结束前必须真实启动、创建页面、执行一段离线操作；主引擎失败时有经过预热的二级引擎兜底。
- **Windows 浏览器关闭后不一定立即释放目录。** 如果先从 staging 启动验证，再把 staging 改名到正式目录，可能遇到 `Access denied`。正确顺序是先备份旧目录、把新目录放到最终路径、再从最终路径启动验证；目录替换和清理做有限重试，失败恢复旧目录。
- **库默认缓存不是产品缓存。** 默认 `ms-playwright`、`~/.cloakbrowser`、PATH 浏览器会让正式/测试串数据，也会随启动方式改变。产品按通道固定缓存、下载和 profile 目录，卸载只删除自己拥有的目录。
- **旧缓存不是兼容兜底。** SDK 升级后旧 revision 即使文件完整也可能拒绝启动。readiness 必须查当前 SDK 精确可执行路径并真启动；旧 revision 只作为待清理诊断信息。
- **安装脚本设置过变量，不代表桌面双击能继承。** GUI、CLI、worker 和 Renderer 每个入口都要重新固定数据、缓存、二进制和自动更新策略；冻结包在 import 业务模块前先清危险继承变量。
- **用户自定义环境变量会把产品带到别处。** `*_BINARY_PATH`、`*_VERSION`、`*_CACHE_DIR`、`*_SKIP_CHECKSUM` 可能来自旧项目。用户版只接受产品命名空间下的显式覆盖，第三方危险变量默认清理。
- **Windows ARM64 不能因为依赖只列 AMD64 就直接判死。** 先确认 x64 应用能否由系统仿真运行，再把第三方平台键映射到真实可用的 windows-x64 资产；确实不支持时在安装期说明，不要到扫码时才报错。
- **重新登录复用旧 context，会出现一次能扫、第二次必死。** 每次扫码先停止旧会话；“重新扫码/重新登录”还要删除当前通道 profile、cookie 和 fallback profile；死 context 只自动重建一次，旧 worker 退出必须释放任务。
- **删除账号只删业务行，不清浏览器 profile，扫码仍会带出旧账号。** 登录态文件也是账号数据的一部分。账号删除/退出要按产品语义清理当前账号 profile 与 cookie，不能碰其他账号或另一通道。
- **检测到发布成功后仍让浏览器停着，会像卡死。** 自动化应识别明确成功状态，先保存结果和归档，再主动关闭本次会话；无法确认时保留可诊断状态，不能把“页面还开着”当成功判据。

### 13.5 冻结打包、桌面入口与图标

- **GUI 冻结后可能没有 stdout。** 日志代码若直接访问 `sys.stdout.buffer`，窗口程序会在 import 阶段报 `NoneType has no attribute buffer`。先修复标准流或退到文件 logger，所有 import-time 日志都要在无控制台 smoke 中验证。
- **开发机有的模块，PyInstaller 不一定自动收。** `_sqlite3`、动态子模块、Pillow 色彩模块、Playwright driver、pywebview 平台后端等需要显式 hidden import/data 收集；冻结 smoke 要扫描 `Traceback`、`ModuleNotFoundError`、`ImportError`。
- **主程序能开不等于整包可用。** Renderer、OCR、转码器等辅助程序必须显式进入包，并在每个平台真实生成最小产物；只跑 `--help` 或 `--list-styles` 会放过缺浏览器 revision 的坏包。
- **macOS 一个 `.app` 放多个可执行文件时，启动目标可能选错。** `CFBundleExecutable` 必须显式等于 GUI 应用名；CI 读取 Info.plist 后直接执行该路径做桌面 smoke，防止 Finder 点图标却启动 Renderer。
- **Mac 图标受 bundle 元数据和 LaunchServices 缓存双重影响。** 包内 `.icns`、bundle id、版本和启动程序要一致；安装替换后刷新注册，否则启动台可能继续显示旧图标。
- **Windows 图标至少有三层。** exe 资源、安装目录/快捷方式和资源管理器图标缓存都要一致；只换 PNG 不会自动改变 `.exe` 或旧快捷方式。
- **自包含不等于完全没有系统前提。** pywebview 仍依赖系统 WebView，进程需要当前用户目录写权限、磁盘和网络。启动时检测最小前提，壳失败可退回本机浏览器；缺前提时给可操作提示。
- **签名不能被应用名替代。** 正式、测试要有不同 bundle id/产品身份；大规模分发还应叠加 macOS 签名与公证、Windows Authenticode。未签名开发包只能作为明确的阶段性状态，不能写成已经具备生产签名。

### 13.6 Windows 与跨平台边界

- **Windows 脚本最容易在用户第一步失败。** 安装、更新、卸载的 `.ps1/.cmd` 保持 ASCII-only，发布前在 PowerShell 5.1 做 parse smoke 和假 OSS 完整安装事务测试。
- **stderr 不等于进程失败。** PowerShell 5.1 配合 `$ErrorActionPreference = "Stop"` 会把原生程序写到 stderr 的普通日志升级为 `NativeCommandError`。捕获输出时临时关闭终止行为，保存 stdout/stderr，再以 `$LASTEXITCODE` 和明确崩溃特征判断结果。
- **中文 JSON 放命令行参数会被 shell、引号和 GBK 联手破坏。** skill 和 CLI 优先 `--data-file`、`--json-file` 或 stdin；不要把多行 JSON、Bash heredoc、`/tmp` 示例写成跨平台主路径。
- **路径永远要假设有空格、中文、OneDrive 和受控目录。** subprocess 使用参数数组，不手拼 shell 字符串；临时目录、安装目录和 profile 目录都做带空格路径测试。
- **控制台编码只影响显示时，不应污染业务数据。** 文件与 HTTP 明确 UTF-8；结构化 CLI 在兼容场景可 `ensure_ascii`；排障先比对字节在哪一层开始损坏，再决定是否是 GBK。
- **跨平台包名不能由当前机器猜。** Windows 资产可能是 `.zip`，macOS/Linux 是 `.tar.gz`；镜像脚本按目标平台资产清单解析，并用发布契约固定扩展名和哈希。
- **“Mac 测过”不能替代 Windows 冻结包测试。** Windows 原生系统构建节点是发布阻断项，必须用目标架构 Python 打包，读取 PE 架构，解压真实 zip、跑版本、桌面初始化、日志扫描、浏览器 setup 和 Renderer 产图。ARM64 Windows 打 x64 包时必须让 PyInstaller 运行在 x64 Python 中，不能把 ARM64 产物改名冒充 AMD64；`platform.machine()` 可能报告宿主 ARM64，可靠判断应读取 Python 和产物的 PE machine（AMD64=`0x8664`）并检查 64 位指针。

### 13.7 安装、更新与卸载事务

- **只更新 CLI 或只更新 UI，用户会得到版本拼盘。** 所有更新入口都委托同一通道安装器，客户端、CLI 入口、skill 和精确运行时一起更新。
- **运行中的应用不能原地覆盖自己。** 应用只负责拉起外部更新脚本/进程，然后退出；新包在 staging 校验通过后再替换，避免 Windows 文件锁和半更新。
- **重复安装每次下载整包，会白白消耗 OSS。** 先拉小 manifest，对比本地版本和健康状态；已最新且健康就跳过大包，但仍修复 skill 和依赖。强制重装使用显式开关。
- **旧安装不是可信依赖来源。** staging 必须使用新包自己的 CLI 准备并真验新 revision 浏览器、Renderer 和 skill；不能先覆盖应用，再拿旧缓存“补齐”。
- **只备份应用、不备份 skill，回滚后仍会版本错配。** 应用、CLI 入口和当前通道 skill 一起备份、提交、回滚；最终路径再做一次桌面初始化和真实辅助任务。
- **卸载为了“彻底”删除全局缓存，会伤害其他软件。** 默认保留业务数据和平台令牌，只清当前产品当前通道拥有的应用、skill、快捷方式和可重建运行时；显式 purge 才删数据。
- **测试版共用正式目录，就不可能安全并存。** 应用名、bundle id、命令、数据、token、调度库、浏览器、profile、skill、manifest、OSS 路径和快捷方式都要隔离。

### 13.8 构建与发布

- **下载源不统一会制造“发布成功”的幻觉。** 构建节点只负责产物，OSS/CDN 负责对外分发，平台页面只展示正式分发入口；用户路径不能混入 GitHub Release/Raw、构建机共享目录或临时服务器。
- **云构建不是默认答案。** 如果有受控的高性能本机和真实目标系统虚拟机，可以用 SSH 编排本地构建，节省 CI 费用；但必须保留云 CI 原本承担的隔离、同源、工具链固定、原生系统测试、完成标记和失败阻断，不能退化成两台机器手工点命令。
- **会自动触发的云 workflow 本身就是成本。** 决定转本机构建后，要删除或禁用旧 push/dispatch 打包配置，避免“双轨同时跑”；可追溯性留在哈希、commit、源码指纹和不可变路径。
- **同一提交不等于同一产物。** 依赖未锁、构建镜像漂移、运行时下载 latest 都会让测试包和正式包不同。完整锁文件、固定构建工具和精确外部资产版本是同产物的前提。
- **动态版本源码变了，editable 元数据仍可能是旧的。** 构建时强制重装本项目包元数据，并断言包管理器看到的版本等于运行时代码版本；否则 wheel 名、marker、客户端 `--version` 会各说各话。
- **PowerShell 会二次解释原生程序参数。** 不要从 PowerShell 用复杂的 `python -c` 内联代码传递嵌套引号、JSON 或路径；看似被单引号包住的 Python 字符串仍可能在 Windows 原生命令行边界丢掉引号。把逻辑放进随源码归档的独立 `.py` 文件，再让两端调用同一文件，并在真实 Windows 中执行。
- **各平台分别成功，不代表属于同一批构建。** 每个平台必须消费同一份源码归档；全部成功后才组装完整干净制品树并写包含 channel/version/commit、归档 SHA、工具链和平台列表的 `build-evidence.json`。全局 Skill 必须与已提交 provenance 对账，防止混用旧 Windows 包和新 Mac 包。
- **canonical manifest 是用户流量开关，项目无权写。** 全局发布 Skill 先 prepare 不可变 OSS 与节点制品，真实客户端双源验收通过后再原子切 canonical。不建立项目自有的另一套固定别名。
- **固定官网 URL 不是固定版本 URL。** 官网和用户命令只能引用平台长期入口；平台选择器从 canonical manifest 找当前不可变脚本。每次普通发版只切 canonical，同一个公网 URL 自动选择新版，不同步改页面、数据库或 Nginx。
- **节点换域名不能直接搜索替换历史 URL。** 先收敛所有发布 ledger，再同步签发新回执和受保护 Profile；客户端继续按 `download_urls` 动态取地址。历史 manifest 与发布记录保持当时事实，新域名只进入后续新版本，OSS fallback 不变。
- **节点“对象存在”不代表支持完整下载。** 发布验收必须对精确版本、全部支持平台的应用与 Playwright/CloakBrowser 制品做真实 Range 与全量 SHA-256 检查，并验证节点 404/超时/坏内容后 OSS 仍可用。
- **测试版改完后直接发正式版，会让测试结论失效。** 任何客户端、CLI、skill、安装更新卸载或发布脚本改动，都要重发测试版。用户明确确认后不改代码，再用同一编排器/spec/lock/工具链构建正式通道。
- **只测源码不测冻结包，会放过最昂贵的错误。** 每个平台先跑全量测试，再对 zip 内 GUI、CLI、Renderer、数据库、浏览器 setup 和真实产物做 smoke；Windows 未运行或失败时整个发布失败。

### 13.9 排障方法

- **“只在 Windows 偶发”不是 Windows 根因的证据。** 先按上游原文、HTTP JSON、解析字段、数据库、UI、渲染输入逐层比对；只有字节在文件、进程或控制台边界开始损坏，才归因编码。
- **第二次重试正常，不等于问题已经修好。** 这通常说明上游输出、网络节点、并发状态或浏览器会话有随机性。记录 request_id、阶段和原始短摘要，找出第一次与第二次的第一处分叉。
- **报错只有一句，无法维护；报错塞满 UI，又无法使用。** 用户界面显示短错误码、原因和下一步；本机日志记录完整堆栈、通道、版本、平台、运行时路径和 request_id，且自动脱敏。
- **文件存在检查容易给假绿。** 对数据库要做读写与迁移，对浏览器要创建页面，对 Renderer 要产图，对安装要最终路径复验，对更新要回滚演练。验收尽量靠最小真实动作，而不是目录扫描。
- **修复后只加说明，问题还会回来。** 每个已确认根因至少落一个自动化约束：单元测试、跨边界契约、发布检查、目标平台冻结包 smoke 或安装事务 smoke；否则它只是一段记忆。

---

> 维护约定：这份是跨项目母本，只沉淀可迁移的理念、骨架和流程。某个具体员工的字段全表、接口错误码全表、版本记录、临时决策，放在各自项目文档里，不往母本里灌实例细节。
