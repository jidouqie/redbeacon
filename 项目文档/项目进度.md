# RedBeacon · 项目进度

> 更新日期：**2026-06-25** ｜ 仓库：`~/code/auto-redbook/redbeacon`（独立 git 仓）。
> 双层：代码层（根目录 `cli/` + `.claude/commands/`）+ 文档层（本 `项目文档/`）；双窗口管理（代码窗口 + PM 窗口）。

## 1. 里程碑看板

| 阶段 | 状态 | 说明 |
|---|---|---|
| 重构方案定稿 | ✅ | 数据归属 / 飞书四表 as-built / 选题 / 仿写 / 计费登录接入 / skill 改造，见 `RedBeacon-重构开发方案.md` |
| 平台接入规范 | ✅ 已出（2026-06-25）| 客户端接 bytestaff 后台逐条契约，见 `RedBeacon-平台接入开发规范.md` |
| 平台后端（外部依赖）| ✅ 已上线在产 | bytestaff T0–T5 全绿、生图网关真出图验过、checkin 契约冻结（`features` 块选 B、⏳ 待平台实现）|
| **T6 客户端改造** | 🚧 进行中（2026-06-25 开工）| 账号对接层 `platform-account` 骨架已落；待接 skill 真跑 login→checkin→生图 |

## 2. 当前阶段：T6 客户端改造

### 已落（骨架）
- **`platform-account` 模块**（与小红书业务逻辑**物理隔离**、可单独拎出做将来共享 SDK 雏形）：login device flow / checkin 轻打卡解析会员摘要 / Bearer 调用（`account_id` 服务端解）/ §9 错误码映射 / doctor 自检。
- 生图改为**带令牌调平台** `/v1/images/generations`；免费能力维持走宿主（铁律 #6）。

### 待办
- 骨架接现有 skill **真跑全链路**：login → checkin → 生图。
- **路 B 落地**：`generate` = 渲染 + 入库 + 出图；文案 JSON 由宿主写好传入（CLI 不调文案 API）。
- **config 重构**：移除 `ai_api_key / ai_base_url / ai_model / image_model`；命名去冲突（`redbeacon login`=平台 vs `redbeacon xhs-login`=小红书）。
- **弃解锁码**：下线 `services/license.py` 离线强制点。
- **号数软控**：读 checkin 下发的 `entitlements[].features.max_xhs_accounts`（free1/Pro3/Max5）。
- 令牌存**共享账号目录**（非 redbeacon 私有）——为将来多员工 skill 共享登录态。

### 依赖平台侧（已知）
- ✅ 平台后端上线、checkin 契约冻结、付费闭环真金白银验过。
- ⏳ checkin **`features` 块**（号数下发，选 B）待平台开发实现——客户端号数软控读它。
- ⏳ device flow / 生图**逐字 JSON schema** 建议平台像 checkin 那样回报冻结、补进接入规范 §2/§4。

## 3. 后续阶段（方案 §7 phasing）

- **P1 UX 深度**：飞书选题+灵感合表、选题 brief 升级、账号策略表。
- **P2 增长**：仿写/抄爆款（v1 提取范围 A+B；mimic 软解锁待平台定归档 + checkin 下发）。
- **P3 远期**：数据复盘付费版、数字员工市场。

## 4. 注意（与本仓边界）

- 本仓 = RedBeacon 客户端（CLI/skill），**不含平台后端**。计费/登录/生图后端在 bytestaff 平台仓（外部依赖）。
- 接平台契约真源 = `RedBeacon-平台接入开发规范.md` + bytestaff 平台文档；本层不重复后端设计。
