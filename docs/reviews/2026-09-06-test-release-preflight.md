# 测试版发布前检查（2026-09-06）

用户请求发布测试版，正式版未获授权。本次没有构建、提交、上传制品或切换线上版本；原有安装入口和全局发布 Skill 均未修改。

## 已完成

- Windows 构建虚拟机 SSH 可达，系统版本为 Windows 10.0.26200.9168。
- CLI 离线全量测试首轮为 1,114 通过、4 失败。修复后在隔离数据目录重跑，**1,118 项全部通过**；未执行 `tests/smoke` 的在线测试或真实小红书发帖。
- 安装回滚回归 5 项、通道证据回归 9 项通过。它们不是新版本冻结包/双平台完整安装验收凭证。
- 两仓 `git diff --check` 通过。未提交其他任务已有的修改，未改变真实业务数据。

## 本次补充修复

1. 代理迁移读取前补齐旧账号表可能缺少的 `proxy` 字段；缺失时默认为空且不启用代理，已有字段和数据保持原样。旧库回归增加字段及重复迁移检查。
2. 审稿页面测试不再要求“AI 声明强制开启、不能关闭”，改为验证可手动开关和两种显示状态；实际前端交互已有独立测试。
3. 默认关闭开关测试不再引用已移除的全局代理轮换配置，使用现存的原创声明默认关闭配置验证同一行为。
4. 超时截图测试使用可持续返回的模拟时钟，避免日志调用全局 `time.time()` 时耗尽迭代器并抛出 `StopIteration`；截图路径改用临时目录。

## 发布工具存在协议冲突

项目构建输出已按单阶段安装组织：

- `tools/check_release_artifacts.py:244` 要求 install 的 `internal_helper_path`、`internal_helper_sha256` 和 `observed_core_request_path` 均为 `null`。
- `tools/write_channel_isolation_receipt.py` 对 install 写入上述单阶段证据；uninstall 仍保留独立 helper。
- 当前项目规则禁止安装时下载 `install-core.*` 或启动第二个安装 Shell。

全局 `bytestaff-digital-employee-publish` 的现行发布前检查仍强制两阶段安装：

- `scripts/preflight_release.py:1079` 对 install/uninstall 均要求存在 `<operation>-core.*`，且观察记录必须显示下载该 helper。
- `scripts/preflight_release.py` 后续还要求公开入口调用 helper；未提供 RedBeacon 单阶段安装分支。
- Skill 自有八入口信任表仍绑定旧安装入口字节，四份现有安装入口与受信任摘要不同。

因此当前项目输出不可能同时通过两套协议；仅更新四个摘要也不能解决。不生成伪造 helper/下载记录、不改回被禁止的两阶段安装，也不跳过发布门禁。

继续发布需要授权对**全局发布 Skill 中 RedBeacon 的单阶段安装兼容**进行适配，保留固定通道、原始报告、独立摘要及跨通道保护；经真实 Mac/Windows 验证后再更新受信任入口摘要。该工作涉及共享发布工具，不按普通产品代码修复自行扩展。

适配获批后仍需完成：源码与版本提交及 provenance 绑定、唯一构建入口的 Mac/Windows 冻结包与安装验收、统一发布流程上传和测试通道切换。不得把本次离线测试通过写成测试版已发布。

## 继续处理授权

用户了解影响后选择“1”：允许仅针对 RedBeacon 适配全局发布工具，保留安全校验，完成双平台验证后发布测试版；正式版保持不变。准备版本为 0.1.122，CLI 来源为 `f0ef33d9ba2c9dbec6a3f8a995310afe632ee9e4`。以上检查记录是适配前的历史现场，不是当前发布成功证明；最终结果以统一发布器的实际回执为准。

## 双平台实测补充修复

- Windows 首轮有 10 个前端测试因系统 GBK 编码无法传递 Unicode JavaScript 失败。三个测试文件改为显式 UTF-8 读文件及子进程输入/输出，保留原断言。CLI 新提交为 `76647ebc07b8316e6116a6bc6f22867fd94fce06`；重跑 Mac 全部 1,118 项通过，Windows 1,115 项通过、3 项平台相关跳过。两端冻结包启动和实际 PNG 渲染检查已通过。
- Windows 安装验收中发现短生命周期卸载 helper 偶发漏记。监测器在 `Start()` 异步订阅后使用 `WaitForNextEvent()` 新建同步订阅，后者没有读取前一订阅的历史事件。已按 [Microsoft 的异步事件契约](https://learn.microsoft.com/en-us/dotnet/api/system.management.managementeventwatcher.start) 改为先注册 `EventArrived`，执行后读取同一队列并清理订阅。
- 增加 Windows 独立行为回归：顺序创建 0、1、2、1、0、2 个快速结束的 PowerShell 子进程，逐个比对实际 PID，并验证没有残留订阅。新监测器六组通过；同一测试对旧监测器复现两个进程只记录一个。主安装验收现在必须先通过此回归。
- 上述只改测试及证据采集，不改公开安装/卸载字节、不生成假进程记录、不放宽单阶段安装断言。此前构建因验收失败未上传；需要从新根提交重新运行完整双平台构建。全局发布 Skill 的四个安装信任摘要仍须等两端完整验收通过后才可更新。
