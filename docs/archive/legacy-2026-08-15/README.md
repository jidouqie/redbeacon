# 2026-08-15 历史资料归档

这里存放已经完成使命、被当前实现取代，或只具有历史追溯价值的资料。归档不会被客户端、CLI、skill、构建器或发布器读取。

归档内容包括：

- `planning/`：宿主 AI 生成能力的已完成实施蓝图与当时的技术验证；最终规则已经进入当前代码和 `AGENTS.md`。
- `tooling/`：被生成脚本和当前维护规范取代的手工 skill 映射表。
- `cli-legacy/`：已经退出产品的离线发码说明与早期飞书 UI 冒烟基线。
- `release-receipts/`：超出近期活动窗口的历史发布回执；当前发布契约和近期回执仍保留在原路径。

本目录不是当前文档来源。任何现行产品行为、数据路径、安装命令、构建流程和发布决策，都必须以仓库根目录的 `AGENTS.md`、当前代码、`docs/README.md` 所列活动文档及受保护发布契约为准。

本次归档刻意没有移动：

- 下载节点接入、项目回执和 `release/release-contract.json`；
- `release/legacy-upgrade-bridge/` 中仍服务老客户端升级的兼容桥；
- 当前三次完整发布及尚未完成发布的回执；
- 当前产品 README、测试版验证指南与本地双平台构建说明。
